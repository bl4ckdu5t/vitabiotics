<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="author" content="ostrich-dev">
	<link href='http://fonts.googleapis.com/css?family=Vollkorn' rel='stylesheet' type='text/css'>
	<%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
	<link rel="shortcut icon" href="<%= "#{root_url}favicon.ico" %>">
	<title>Log in - Vitabiotics</title>
</head>
<body>
	<% flash.each do |name, msg| %>
			<%= content_tag :div, msg, :id => "flash_#{name}", class: 'login-alert' %>
	<% end %>
	<div class="login-form">
		<div class="top-colors"><div></div><div></div><div></div><div></div></div>
		<div class="login-header"></div>
		<%= form_tag sessions_path, class: 'clearfix' do %>
			<%= label_tag :email %>
			<%= text_field_tag :email, params[:email], placeholder: 'Email', type: 'email' %>
			<%= label_tag :password %>
			<%= password_field_tag :password, params[:password], placeholder: '******' %>
			<button class="fr" type="submit">Log In</button>
			<span><a href="">Forgot Password?</a></span>
		<% end %>
	</div>
	<!--<div class="login-links"><a href="<%= root_url %>">Back to website</a></div>-->
	<!-- Scripts -->
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
</body>
</html>